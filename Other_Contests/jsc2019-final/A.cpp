#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for(int(i)=0;(i)<(n);(i)++)

typedef vector<long long int> vll

int main() {
  int n, m;
  cin >> n >> m;
  vll A(n);
  vll B(m);

  rep(i, n) cin >> A[i];
  rep(i, n) cin >> B[i];

  
}