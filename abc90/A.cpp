#include <bits/stdc++.h>
using namespace std;

int main() {
  for(int i=0;i<9;i++) {
    if(i==0 || i==4 || i==8) {
      char a;
      cin >> a;
      cout << a;
    } else {
      char a;
      cin >> a;
    }
  }
  cout << endl;
}